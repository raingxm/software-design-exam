# 子网划分（进行中）

> 第 10 章 · 上午手算高频  
> 开场：[01-network-entry](../10-network/01-network-entry.md) · [交接看板](./01-learning-logs.md)  
> 状态：🔄 2026-08-18 开课 · 辨型尚未过关

---

## 总口令

```text
/n 的 n = 网络位个数；剩下 32-n = 主机位
块大小 = 2^主机位（末字节里一跳多长）
网络号 = 该块的起点（主机位全 0）
广播  = 该块的终点（主机位全 1）
可用主机 = 块大小 - 2
同网 = 网络号相同
```

---

## 一、一条龙：192.168.1.100/26

`/26` → 网络位 26，主机位 6。  
块大小 `2^6 = 64`。掩码 `255.255.255.192`（256-64=192）。

末字节按 64 一跳切：`0 · 64 · 128 · 192`（再到 256 越界）。

100 落在 **64～127** 这一块：

| 项 | 值 |
|----|-----|
| 网络号 | 192.168.1.**64** |
| 广播 | 192.168.1.**127** |
| 可用 | .65 ～ .126 |
| 可用台数 | 64 - 2 = **62** |

130 落在 128～191，网络号是 `.128`，和 `.100` **不同网**。

---

## 二、常考掩码（末字节）

| /n | 掩码末位 | 块大小 | 可用主机 |
|----|----------|--------|----------|
| /24 | 0 | 256 | 254 |
| /25 | 128 | 128 | 126 |
| /26 | 192 | 64 | 62 |
| /27 | 224 | 32 | 30 |
| /28 | 240 | 16 | 14 |
| /30 | 252 | 4 | 2 |

从 /24 再借 k 位 → 子网个数 `2^k`。例：/24 划成 /26，借 2 位，**4** 个子网。

A/B/C 类默认：A=/8、B=/16、C=/24。私网了解：`10/8`、`172.16/12`、`192.168/16`。

---

## 三、易混

| 对比 | 怎么分 |
|------|--------|
| 网络号 vs 第一个可用 | 网络号主机位全 0，**不能**分给主机；下一号才可用 |
| 广播 vs 最后一个可用 | 广播全 1，**不能**分给主机；前一号才可用 |
| 同网 | 只比网络号，不要只看「看起来都叫 192.168.1.x」 |

---

## 四、本次练习（答案先不写，对话里对）

题面一律用 **192.168.1.100/26** 当参照（上一节已算过的可直接用）。

1. `/26` 的掩码是什么？块大小是多少？
2. 192.168.1.100/26 的**网络号**和**广播地址**？
3. 该网**可用主机数**？第一个和最后一个可用地址？
4. 192.168.1.100/26 和 192.168.1.130/26 同网吗？为什么？
5. 把 192.168.1.0/24 划成 /26，能划几个子网？
