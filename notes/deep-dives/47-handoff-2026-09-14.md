# Software Designer exam study handoff

## Next-session focus

Continue with the Java 五坑 (five-pitfall) coverage problem. Strategy 15/15 (9/9) and Facade 15/15 (9/14) are done but neither directly hits 私有构造器"克隆站" / 深浅克隆 / 原生集合 API / `Object` 强转. Pick one candidate and do it closed-book, then resume the 2024 上半年 official morning paper work that was the main line on the 9/11 handoff.

## Repository state

- Workspace: `D:\repos\software-design-exam`
- Branch: `main`
- Latest commit on this handoff: previous one was `79c105c docs: update handoff with python exceptions verified and 4nf lecture`; this handoff will add a new commit on top.
- Existing study state and prior verified topics are recorded in:
  - `NOTES.md`
  - `practice/past-papers/2024/2024-first-half-question-bank-baseline.md`
  - `notes/deep-dives/01-learning-logs.md`
  - Topic notes under `notes/`
- The 9/11 handoff (`notes/deep-dives/46-handoff-2026-09-11.md`) is still authoritative for the **morning question bank** work; this handoff only documents the 9/14 Java 外观 session.

## Today's session (2026-09-14)

### Java 外观模式 (Facade) — closed-book 15/15

Source: official-style 试题六, Facade 接待员 / 挂号 / 门诊 / 取药 (year TBD; user brought the problem, not committed to a specific session).

Class diagram read:

- `Patient` (interface) — `ConcretePatient` (implements Patient) via generalization
- `Facade` associates `Patient` and `Disposer`
- `Disposer` (interface) — `Registry`, `Doctor`, `Pharmacy` (all implement Disposer)

Blanks user filled, all correct, no syntax errors:

1. `public String getName()` — declared in `Patient` interface
2. `public void dispose(Patient patient)` — declared in `Disposer` interface
3. `new ConcretePatient("test")` — concrete creation
4. `Facade` — type of local variable `f`
5. `new Facade(patient)` — Facade constructor takes Patient
6. `f.dispose()` — triggers the Facade sequence (registry → doctor → pharmacy)

### Key takeaway for the next session

**Don't add `;` inside a blank when the question stem already places one after it.** User's first instinct was correct, and the teaching correction came from the assistant misreading the screenshot. The rule is: 软考 Java 填空，看空外是否已带分号——带了就不写，没带就补.

### 五坑 still NOT covered (this is why next session picks 享元 / 装饰 / 工厂方法)

| Pitfall | 策略 9/9 | 外观 9/14 |
|---|---|---|
| 私有构造器"克隆站" | ❌ | ❌ |
| 深浅克隆 | ❌ | ❌ |
| 原生集合 API（`Hashtable` / `HashMap` / `Vector`） | ❌ | ❌ |
| `Object` 返回值强转 | ❌ | ❌ |
| 接口方法 + `implements` + 构造器带参 | ✅ | ✅ |

Pattern recognition is now stable across two distinct patterns. Next Java problem must attack the **syntax/API** layer, not the pattern-recognition layer.

## Next-session entry point

1. Open one of: 2021 下 享元（围棋棋子）/ 2019 上 享元（字符）/ 2018 上 装饰 / 2020 下 工厂方法. Preferred: 2021 下 享元 — the `FlyweightFactory` returning `Flyweight` with a `Hashtable` is a direct five-pitfall composite.
2. Closed-book first pass, then verify against the official answer key in the same source.
3. Update `01-learning-logs.md` 进度快照 and TODO 10.
4. Then resume the 9/11 main line: 2024 上半年 official morning paper, blocks of 25 timed.

## Teaching preferences (unchanged from 9/11 handoff)

- Chinese, concrete examples first, formulas/terminology second.
- One concept cluster at a time; short verification questions.
- The user explicitly notices omitted syllabus points, so acknowledge genuine coverage gaps and add them to notes later instead of calling everything careless error.
- Keep App numbering separate from official paper numbering.
- Avoid unexplained jargon and long answer dumps.
