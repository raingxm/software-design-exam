# Software Designer exam study handoff

## Next-session focus

Resume the 2024 上半年 official morning paper work that was the main line on the 9/11 handoff. Java 模式识别已经三道（策略 15/15 / 外观 15/15 / 享元 12/15）全部稳定，五坑仅剩"克隆站 + 深浅克隆 + `Object` 强转"三坑——下一题 Java 选有这三坑的（首选原型题再战、备选工厂 `Object` 强转）。

## Repository state

- Workspace: `D:\repos\software-design-exam`
- Branch: `main`
- Latest commit on this handoff: previous one was `25762de docs: record facade pattern 15-15 and hand off 2026-09-14`; this handoff update will add a new commit on top.
- Existing study state and prior verified topics are recorded in:
  - `NOTES.md`
  - `practice/past-papers/2024/2024-first-half-question-bank-baseline.md`
  - `notes/deep-dives/01-learning-logs.md`
  - Topic notes under `notes/`
- The 9/11 handoff (`notes/deep-dives/46-handoff-2026-09-11.md`) is still authoritative for the **morning question bank** work; this handoff documents the 9/14 Java 外观 + 享元 session.

## Today's session (2026-09-14)

### Java 外观模式 (Facade) — closed-book 15/15

Source: official-style 试题六, Facade 接待员 / 挂号 / 门诊 / 取药 (year TBD; user brought the problem, not committed to a specific session).

Class diagram read:

- `Patient` (interface) — `ConcretePatient` (implements Patient) via generalization
- `Facade` associates `Patient` and `Disposer`
- `Disposer` (interface) — `Registry`, `Doctor`, `Pharmacy` (all implement Disposer)

Blanks user filled, all correct, no syntax errors:

1. `public String getName()` — declared in `Patient` interface
2. `public void dispose(Patient patient)` — declared in `Disposer` interface
3. `new ConcretePatient("test")` — concrete creation
4. `Facade` — type of local variable `f`
5. `new Facade(patient)` — Facade constructor takes Patient
6. `f.dispose()` — triggers the Facade sequence (registry → doctor → pharmacy)

### Java 享元模式 (Flyweight · 五子棋) — closed-book 12/15

Source: **2021 年下半年 下午试题六**（用户自带；真题存档 `practice/past-papers/2021/2021-second-half-q6-flyweight-gomoku.md`）。

Class diagram read:

- `PieceBoard` ◇—> `Piece`（聚合，棋盘持有多颗棋子）
- `Piece` —> `PiecePos`（每颗棋子持有位置）
- `BlackPiece` / `WhitePiece` △—> `Piece`（泛化 / 继承）

Blanks user filled (4/5 correct, (1) 漏 `abstract`):

1. `public void draw();` — **MISSING `abstract` keyword**, should be `public abstract void draw();`
2. `Piece` — `ArrayList` 的类型参数
3. `Piece` — 局部变量类型
4. `piece.draw()` — 放完黑子调用绘制
5. `piece.draw()` — 放完白子调用绘制

### 今日两条新口令

- **看空外分号**：软考 Java 填空，看空外是否已带分号——带了就不写，没带就补。
- **`abstract class` 内无方法体的方法 = 抽象方法 = 必须显式标 `abstract`**。`public void draw();` 既是语法错又扣分。

### 五坑覆盖进度（截至 9/14）

| 风险点 | 策略 9/9 | 外观 9/14 | 享元 9/14 |
|---|---|---|---|
| 看空外分号 | n/a | ✅ | ✅ |
| 抽象方法必带 `abstract` | n/a | n/a | ⚠️ 扣 3 分（已碰到） |
| 原生集合 API（`ArrayList` + `add()`） | ❌ | ❌ | ✅ |
| 接口 / 抽象类 + `implements`/`extends` + 构造器带参 | ✅ | ✅ | ✅ |
| 私有构造器"克隆站" | ❌ | ❌ | ❌ |
| 深浅克隆 | ❌ | ❌ | ❌ |
| `Object` 返回值强转 | ❌ | ❌ | ❌ |

→ 剩余高危三坑：**克隆站 / 深浅克隆 / `Object` 强转**。

## Next-session entry point

1. **（Java 主线）** 挑一道覆盖剩余三坑的题：首选 **2024 上半年 q6 原型模式再战**（已有官方题，2024 上 q6 是用户 9/2 原型 1.5/5 那道，本次重做时已带五坑口令复测）；备选任何含 `Object` 强转 + 私有构造器的工厂题。
2. **（主线）** 然后切回 9/11 主线：2024 上半年 official 上午题 PDF，按 `3 × 25` 限时块做。
3. 更新 `01-learning-logs.md` 进度快照和 TODO 11。

## Teaching preferences (unchanged from 9/11 handoff)

- Chinese, concrete examples first, formulas/terminology second.
- One concept cluster at a time; short verification questions.
- The user explicitly notices omitted syllabus points, so acknowledge genuine coverage gaps and add them to notes later instead of calling everything careless error.
- Keep App numbering separate from official paper numbering.
- Avoid unexplained jargon and long answer dumps.
