# Teaching Notes

- 用户偏好围绕错题讨论并继续扩展相关知识点。
- 混合诊断已完成一轮：删除移动个数与链表二分失效独立答对；平均移动次数精确值（插入 n/2、删除 (n−1)/2）经辨析 (n+1)/2 混淆后理解，待变式题验证。
- 不带头结点删首元的头指针改写已讲解确认，待变式题验证后标记掌握。
- 2026-09-10 下午续学顺序：先做 Java 完整新真题 1 道；再收尾算法 C 回溯题（手推 `3 x 3`、最坏复杂度、改写 `found`）；然后用两个短变式验收顺序表平均移动次数与不带头结点删首元的头指针改写。
- 若上述完成且有余力，再进入动态数组摊还复杂度（扩容倍增与摊还 O(1)）。
- 讲解应强调逐步手推与小实例硬枚举，而非孤立记忆代码顺序。
