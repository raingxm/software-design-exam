# 2026 上半年综合知识 · 58 题诊断与错题复盘

## 成绩口径

- **来源**：刷题 App“2026 年上半年—持续更新中”，当前共 58 题，并非完整 75 题官方原卷。
- **完成日期**：2026-09-21。
- **得分**：**46/58（79.3%）**。
- **错题**：共 12 道：`2、7、8、19、21、26、28、31、38、45、55、58`。
- **限制**：实际用时与逐题信心标记尚未记录；题集不完整且缺题未必随机，因此本次只作盲点诊断，不折算为正式 `/75` 整卷成绩。

## 第一批 4 题复盘（2026-09-22）

### Q49 · 指令周期（原题答对）

- 指令周期通常包括取指、执行，并可能包含间址与中断周期。
- 中断周期只有在存在可响应的中断请求时才进入；“每个指令周期都包含一个中断响应机器周期”错误。
- **判定规则**：取指、执行通常必有；间址、中断按条件出现。

### Q50 · NFA 的不确定性（原题答对，概念补牢）

- `ε` 转移允许不读取字符就改变状态。
- 同一状态读取同一字符可以转移到多个下一状态。
- 多个终态并不会造成转移不确定，DFA 也可以有多个终态。
- **即时验收**：三种情形辨析 `3/3`。
- **判定规则**：NFA 看“空转移”和“同字符多去向”，不看终态数量。

### Q55 · 多址复用（原题错误）

- 原答时分复用，正确为码分复用：多个用户在同一时间使用同一频带，通过不同编码区分。
- 已完成频分、时分、码分、波分、空分的第一轮场景辨析，基础验收 `6/6`；保护带与同步/统计 TDM 两道追加题也答对。
- 当前阶段不扩展正交码计算、扩频因子、远近效应与 OFDM/OFDMA 工程细节。
- **判定规则**：不同频带是频分，不同时隙是时分，同时同频但编码不同是码分，光纤不同波长是波分，不同方向或空间信道是空分。

### Q58 · 软件著作权（原题错误，重复薄弱点）

- 为学习、研究软件内含的思想和原理，以安装、显示、传输或存储等方式使用软件，可以不经著作权人许可，也不向其支付报酬。
- 原选项将规则写成“可不支付报酬，但必须经许可”，因此错误。
- 本项在 09-20 复测中曾“答对但犹豫”，本次再次失分，已从犹豫项升级为优先复测项。
- **判定规则**：学习研究软件思想和原理，在法定范围内是“免许可 + 免报酬”，不是只免报酬。

## 第二批 4 题复盘（2026-09-22）

### Q31 · IPv6 目的地址类型（原题错误）

- IPv6 的三种基本目的地址类型为单播、多播、任播，不包含广播。
- 任播是多个候选节点共用地址、路由到其中较近的一个；多播是一发多收。
- **即时验收**：任播与多播场景辨析正确。
- **判定规则**：IPv6“单、多、任，无广播”。

### Q38 · Python `is` 与 `==`（原题错误）

- `==` 比较值是否相等，`is` 判断是否为同一个对象；判断 `None` 使用 `is None`。
- **即时验收**：两个等值列表与一个别名对象的三项判断全对。
- **判定规则**：`==` 看内容，`is` 看对象身份。

### Q39 · RSA（原题答对）

- RSA 是基于大整数分解困难性的非对称加密算法；RC4、DES 是对称加密算法，MD5 是摘要算法。
- 本题保持，不扩展 RSA 运算细节。

### Q45 · 防火墙与 IDS/IPS（原题错误）

- 位于内外网边界、按规则允许或拒绝数据包的是防火墙；IDS 侧重检测告警，IPS 侧重串联检测并阻断。
- **即时验收**：旁路监听、发现攻击并告警的场景正确识别为 IDS。
- **判定规则**：防火墙守门，IDS 报警，IPS 拦截。

## 第三批 5 题复盘（2026-09-22）

### Q23 · 顺序内聚（原题答对，今晚回顾）

- 操作按固定顺序执行，且后一步依赖前一步结果，属于顺序内聚。
- 仅有执行顺序而没有前后数据依赖，更接近过程内聚。
- 用户已理解，指定今晚再复习一次，当前不提前记为间隔保持。

### Q24 · 回溯算法（原题答对，概念扩展完成）

- 回溯沿解空间树深度优先试选，违反约束时撤销并剪枝；动态规划定义状态、保存并复用重叠子问题的结果。
- **即时验收**：八皇后、矩阵链、全排列、最长公共子序列和无缓存 0-1 背包五种场景辨析 `5/5`。
- **判定规则**：回溯避免无效路径，动态规划避免重复计算相同状态。

### Q26 · 时间片轮转退化（原题错误，待即时变式）

- 时间片过大时，每个进程往往在一个时间片内完成，退化为先来先服务；时间片过小则上下文切换开销增大。
- 原答优先级调度，原因是未从“一个接一个运行完成”识别 FCFS。
- 已完成讲解，但尚未回答陌生变式，今晚优先验收。

### Q27 · 等价类划分（原题答对）

- 将输入划分为行为相近的有效/无效等价类，每类选择少量代表值，以减少冗余用例、提高测试效率。
- 不能保证 100% 缺陷发现率，也不能替代系统测试。

### Q28 · 计算机程序相关专利（原题错误，待即时变式）

- 具体源代码表达主要由软件著作权保护；应用算法形成具有技术特征和技术效果的计算机相关技术方案时，可能获得专利保护。
- 按本题选项与考试口径，应选“计算机算法”；不能泛化为任何抽象算法都可获专利。
- 已完成讲解，但尚未回答陌生变式，今晚优先验收。

## 当前状态与下一步

- 12 道原错题中已复盘 7 道：`Q26、Q28、Q31、Q38、Q45、Q55、Q58`；剩余 `Q2、Q7、Q8、Q19、Q21` 共 5 道。
- 已通过即时变式：Q31、Q38、Q45；Q55 已完成五类复用辨析，Q58 已答对委托开发归属变式，但两项仍需间隔保持验证。
- 今晚先复习 Q23 顺序内聚，再对 Q26 时间片退化、Q28 著作权/专利边界各做 1 道闭卷变式；随后继续剩余 5 道错题。
- Q55 与 Q58 自本轮讲解间隔 `48h` 后混入陌生题复测；Q58 若再次失分则升级为知识产权小专题。
