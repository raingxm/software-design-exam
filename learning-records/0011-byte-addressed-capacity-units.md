# 理解编址方式决定存储容量单位

用户理解十六进制地址差得到的是存储单元个数，本身不带 bit 或 Byte 单位；按字节编址时每个地址对应 1Byte，所以 `10000H=64K` 个地址对应 `64KB`。同时能区分 `32K×8bit` 表示 32K 个字节，而非 32Kbit。

## Evidence

在 `A0000H～DFFFFH` 芯片片数题中，用户追问“为什么 `10000H=64K` 后是 KB 而不是 bit”，经“地址数 × 每地址容量”的拆分讲解后明确表示理解；原题由误选 16 订正为 8 片，后续待变式复测。
