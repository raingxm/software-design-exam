# 能追踪赋值后的指针状态

用户已能准确解释双向循环链表插入题中 B 选项的错误：执行 `p->next = s` 后，后续的 `p->next` 已表示 `s`，因此 `s->next = p->next` 会产生 `s->next = s` 的自环。后续教学可以不再停留于本题答案，转向边界条件、删除操作以及数组与链表的综合比较。

## Evidence

用户用自己的话复述了指针覆盖发生的位置和后果。
