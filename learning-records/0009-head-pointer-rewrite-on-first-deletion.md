# 理解不带头结点删首元结点需改写头指针

用户理解不带头结点的单链表删除首元结点必须先 `q = L; L = L->next;` 再 `free(q)`，直接 free 会使头指针悬空并丢失整条链；带头结点后头指针永不修改，删除代码与删中间结点统一。该结论经讲解确认，尚待变式题独立验证。

## Evidence

用户先答"直接 free"，经逐句手推 q、L 指向变化与 n=2 小实例后确认理解，并主动追问头指针与链表本身的区别直至厘清"改头指针"与"改前驱 next 域"的差异。
