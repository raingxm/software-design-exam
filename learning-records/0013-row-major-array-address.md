# 已掌握二维数组行优先地址计算

用户能使用行优先、下标从 0 开始时的公式：`LOC(A[i][j]) = base + (i * 列数 + j) * 元素大小`。能够先由两个已知地址反推出列数，再计算目标元素地址。

## Evidence

2026-09-10 复盘 2024 上半年题库：原题中由 `A[0][0] = 100`、`A[3][3] = 220`、元素大小为 2，订正出元素偏移 `3n+3=60`、列数 `n=19`，进而得到 `A[5][5]` 地址 300。随后对新坐标独立回答元素偏移 39、地址增量 78、最终地址 178，三步全部正确。
